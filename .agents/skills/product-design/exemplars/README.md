# Exemplars

Add `pr-{name}.md` files here when a merged PR is the gold standard for agent UI work.

Each exemplar should include:

- Link or summary of the PR
- Which surface(s) it demonstrates (nav, cards, hero, prose, etc.)
- 2–3 patterns worth copying
- Any rules that were added to `design.md` because of it

No exemplars yet — add the first after a successful agent-built UI lands on `main`.
