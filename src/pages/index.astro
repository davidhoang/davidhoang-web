---
import MainLayout from '../layouts/MainLayout.astro';
import { getCollection } from 'astro:content';
---

<MainLayout title="David Hoang">
  <!-- Hero Section -->
  <section class="hero-grid">
    <div class="hero-content">
      <h1>David Hoang is a designer + investor</h1>
      <p>I'm building and investing in tools that revolutionize the internet. Currently I am VP&nbsp;of&nbsp;Design,&nbsp;AI at <a href="https://atlassian.com" target="_blank">Atlassian</a> and Advisor for <a href="https://play.com" target="_blank">Play</a>. In my free time I advise startups, mentor a select few designers, and design/prototype ideas.</p>
      <p>Quick links:</p>
      <ul>
        <li>Subscribe to my newsletter, <a href="https://www.proofofconcept.pub" target="_blank">Proof of Concept</a></li>
        <li>Buy the zine, <a href="https://davidhoang.gumroad.com/l/proof-of-concept-000-series">Proof of Concept: The 000 Series</a></li>
        <li><a href="https://github.com/davidhoang">Follow me on GitHub</a></li>
        <li><a href="https://youtu.be/piGC-iFwmrk?si=RuQtmVtimws3npVM">Watch my talk from Config 2021</a></li>
        <li><a href="https://www.youtube.com/watch?v=6Z88rLjF-lc">Listen to my interview on Dive Club</a></li>
      </ul>
    </div>
    <div class="hero-image">
      <div class="theme-image" aria-label="Photo of David"></div>
    </div>
  </section>
  <style>
    .hero-content a {
      color: var(--color-link);
      text-decoration: none;
      border-bottom: 1px dotted var(--color-link);
      transition: all 0.3s ease;
    }

    .hero-content a:hover {
      color: var(--color-link-hover);
      border-bottom-color: var(--color-link-hover);
    }

    .theme-image {
      width: 400px;
      aspect-ratio: 3/4;
      background-image: url('/images/img-dh.png');
      background-size: cover;
      background-position: center;
      border-radius: 8px;
      transition: background-image 0.3s ease;
    }

    :global([data-theme="dark"]) .theme-image {
      background-image: url('/images/img-dh-dark.png');
    }

    :global([data-theme="dark"]) #substackEmbed {
      filter: invert(1) hue-rotate(180deg);
    }

    @media (max-width: 480px) {
      .theme-image {
        width: 100%;
      }
    }

    .substack-embed-wrapper {
      background-color: var(--color-bg);
      padding: 1rem;
      border-radius: 8px;
    }

    #substackEmbed {
      background-color: var(--color-bg);
    }

    .newsletter-container {
      background: var(--color-bg);
      padding: 2rem;
      border-radius: 12px;
      border: 1px solid var(--color-border);
      text-align: center;
    }

    h2 {
      font-size: 2.5rem;
      margin: 0 0 1rem;
      font-weight: 600;
      color: var(--color-text);
    }

    p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      color: var(--color-text);
    }

    .input-group {
      display: flex;
      gap: 8px;
      margin-bottom: 1rem;
    }

    input {
      flex: 1;
      padding: 12px 16px;
      border: 1px solid var(--color-border);
      border-radius: 6px;
      background: var(--color-bg);
      color: var(--color-text);
      font-size: 1rem;
    }

    input::placeholder {
      color: var(--color-muted);
    }

    button {
      padding: 12px 24px;
      background: var(--color-text);
      color: var(--color-bg);
      border: none;
      border-radius: 6px;
      font-weight: 500;
      font-size: 1rem;
      cursor: pointer;
      transition: opacity 0.2s;
    }

    .disclaimer {
      font-size: 0.8rem;
      color: var(--color-muted);
    }

    .disclaimer a {
      color: var(--color-link);
    }

    /* Mobile styles */
    @media (max-width: 480px) {
      .input-group {
        flex-direction: column;
      }

      button {
        width: 100%;
      }
    }
  </style>
</MainLayout>