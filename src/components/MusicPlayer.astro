---
import MusicPlayerComponent from './MusicPlayer.tsx';
---

<div data-astro-transition-scope="music-player">
  <MusicPlayerComponent client:idle />
</div>

<style is:global>
  /* Winamp Color Palette - Authentic Classic Winamp Colors */
  :root {
    --winamp-bg: #232323; /* Main charcoal background - the iconic Winamp gray */
    --winamp-bg-panel: #2b2b2b; /* Slightly lighter panel background */
    --winamp-button-bg: #3a3a3a; /* Neutral gray button background */
    --winamp-button-face: #4a4a4a; /* Button face color */
    --winamp-dark: #1a1a1a; /* Dark areas */
    --winamp-darker: #0f0f0f; /* Deepest shadows */
    --winamp-light: #525252; /* Lighter gray for hover states */
    --winamp-lighter: #5a5a5a; /* Even lighter for highlights */
    --winamp-green: #00ff00; /* Bright green LCD text */
    --winamp-green-dim: #00c800; /* Slightly dimmer green */
    --winamp-green-dark: #009800; /* Dark green for inactive elements */
    --winamp-green-glow: rgba(0, 255, 0, 0.4); /* Green glow effect */
    --winamp-orange: #ff6600; /* Orange for stereo indicator */
    --winamp-slider-track: #1e3c1e; /* Dark green-tinted track */
    --winamp-slider-thumb: #a0a078; /* Olive/khaki slider thumb - classic Winamp */
    --winamp-slider-thumb-highlight: #c0c0a0; /* Lighter olive for highlight */
    --winamp-text: #d4d4d4; /* Light gray text */
    --winamp-text-light: #ffffff; /* White text */
    --winamp-text-dark: #000000; /* Black text */
    --winamp-border: #0a0a0a; /* Near-black borders */
    --winamp-border-light: #3a3a3a; /* Lighter border for 3D effect */
    --winamp-highlight: #4a4a4a; /* 3D highlight edge */
    --winamp-shadow: #000000; /* 3D shadow edge */
    --winamp-titlebar-start: #0a0a4a; /* Title bar gradient start */
    --winamp-titlebar-end: #000080; /* Title bar gradient end */
    --winamp-display-bg: #000000; /* Black display background */
    --winamp-selected: #0000c0; /* Blue for selected items */
    --winamp-selected-text: #ffffff; /* White text on selected */
  }

  /* Main Winamp Container */
  .winamp-player {
    position: fixed;
    bottom: 0;
    left: 0;
    z-index: 9999;
    font-family: 'MS Sans Serif', 'Arial', sans-serif;
    font-size: 11px;
    user-select: none;
    transform: translateZ(0);
    will-change: transform;
  }

  .winamp-player.dragging {
    cursor: grabbing !important;
  }

  .winamp-player.dragging * {
    pointer-events: none;
  }

  .winamp-player.dragging .winamp-titlebar {
    pointer-events: auto;
  }

  .winamp-minimized {
    position: fixed;
    bottom: 0;
    left: 0;
    z-index: 9999;
  }

  .winamp-toggle-btn {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-highlight);
    border-left: 1px solid var(--winamp-highlight);
    padding: 4px 8px;
    font-size: 10px;
    font-weight: bold;
    cursor: pointer;
    color: var(--winamp-text);
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
  }

  .winamp-toggle-btn:hover {
    background: var(--winamp-light);
  }

  .winamp-toggle-btn:active {
    box-shadow: inset 1px 1px 2px rgba(0, 0, 0, 0.5);
    border-top: 1px solid var(--winamp-shadow);
    border-left: 1px solid var(--winamp-shadow);
  }

  /* Main Player Window */
  .winamp-main {
    width: 275px;
    background: var(--winamp-bg);
    border: 2px solid var(--winamp-border);
    border-bottom: none;
    box-shadow: 
      inset -1px -1px 0 var(--winamp-shadow),
      inset 1px 1px 0 var(--winamp-highlight),
      2px 2px 4px rgba(0, 0, 0, 0.3);
  }

  .winamp-titlebar {
    background: linear-gradient(to bottom, var(--winamp-titlebar-start), var(--winamp-titlebar-end));
    color: var(--winamp-text-light);
    padding: 2px 4px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 10px;
    font-weight: bold;
    cursor: move;
    border-bottom: 1px solid var(--winamp-border);
  }

  .winamp-logo {
    font-size: 8px;
    letter-spacing: 1px;
  }

  .winamp-title {
    flex: 1;
    text-align: center;
  }

  .winamp-window-controls {
    display: flex;
    gap: 2px;
  }

  .winamp-btn-minimize,
  .winamp-btn-close {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    width: 16px;
    height: 14px;
    font-size: 12px;
    font-weight: bold;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    line-height: 1;
    color: var(--winamp-text);
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
  }

  .winamp-btn-minimize:hover,
  .winamp-btn-close:hover {
    background: var(--winamp-light);
  }

  .winamp-btn-close:hover {
    background: #ff0000;
    color: white;
  }

  /* Display Area */
  .winamp-display {
    background: #000;
    padding: 8px;
    border: 2px inset var(--winamp-dark);
    margin: 4px;
    min-height: 150px;
  }

  /* Enhanced Time Display - LED/Segment Style */
  .winamp-time-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
    background: var(--winamp-darker);
    padding: 4px 8px;
    border: 1px inset var(--winamp-dark);
  }

  .winamp-time-display {
    font-family: 'Courier New', monospace;
    font-size: 20px;
    color: var(--winamp-green);
    text-shadow:
      0 0 8px var(--winamp-green),
      0 0 12px var(--winamp-green),
      0 0 4px rgba(0, 255, 0, 0.8);
    font-weight: bold;
    letter-spacing: 3px;
    display: flex;
    gap: 2px;
  }

  .winamp-digit {
    display: inline-block;
    min-width: 12px;
    text-align: center;
    background: rgba(0, 20, 0, 0.3);
    padding: 2px 4px;
    border-radius: 2px;
  }

  .winamp-digit.colon {
    min-width: 6px;
    background: transparent;
  }

  .winamp-time-remaining {
    font-family: 'Courier New', monospace;
    font-size: 12px;
    color: var(--winamp-green-dark);
    text-shadow: 0 0 4px var(--winamp-green-dark);
    font-weight: bold;
  }

  /* Position/Seek Slider */
  .winamp-seek-container {
    position: relative;
    height: 10px;
    background: var(--winamp-darker);
    border: 1px inset var(--winamp-dark);
    margin-bottom: 6px;
    cursor: pointer;
  }

  .winamp-seek-slider {
    position: absolute;
    width: 100%;
    height: 100%;
    opacity: 0;
    cursor: pointer;
    z-index: 2;
  }

  .winamp-seek-progress {
    position: absolute;
    height: 100%;
    background: linear-gradient(to bottom, #90c090 0%, #60a060 50%, #408040 100%);
    box-shadow: 0 0 2px rgba(100, 180, 100, 0.5);
    transition: width 0.1s linear;
    pointer-events: none;
  }

  .winamp-seek-slider:disabled + .winamp-seek-progress {
    background: var(--winamp-dark);
    box-shadow: none;
  }

  /* Track Info with Marquee */
  .winamp-track-info-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
    height: 16px;
  }

  .winamp-track-info {
    flex: 1;
    color: var(--winamp-green);
    font-size: 9px;
    overflow: hidden;
    position: relative;
    background: rgba(0, 20, 0, 0.2);
    padding: 2px 4px;
    border: 1px solid rgba(0, 255, 0, 0.2);
  }

  .winamp-track-scrolling {
    display: inline-block;
    white-space: nowrap;
    animation: marquee 15s linear infinite;
  }

  @keyframes marquee {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-100%);
    }
  }

  .winamp-track-info:hover .winamp-track-scrolling {
    animation-play-state: paused;
  }

  .winamp-bitrate-display {
    display: flex;
    gap: 6px;
    color: var(--winamp-green);
    font-size: 8px;
    font-family: 'Courier New', monospace;
    background: rgba(0, 20, 0, 0.3);
    padding: 2px 6px;
    border: 1px solid rgba(0, 255, 0, 0.2);
  }

  .winamp-bitrate::after {
    content: 'kbps';
    margin-left: 2px;
  }

  .winamp-khz::after {
    content: 'kHz';
    margin-left: 2px;
  }

  .winamp-spectrum {
    display: flex;
    align-items: flex-end;
    gap: 2px;
    height: 30px;
    margin-bottom: 6px;
  }

  .winamp-spectrum-bar {
    flex: 1;
    min-height: 2px;
    background: var(--winamp-green);
    transition: height 0.1s ease;
    border-radius: 1px;
  }

  /* Volume and Balance Controls */
  .winamp-volume-balance-container {
    display: flex;
    gap: 12px;
    margin-bottom: 6px;
    padding: 4px;
    background: var(--winamp-darker);
    border: 1px inset var(--winamp-dark);
  }

  .winamp-volume-control,
  .winamp-balance-control {
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .winamp-label {
    font-size: 8px;
    color: var(--winamp-green);
    font-weight: bold;
    min-width: 24px;
  }

  .winamp-volume-slider {
    width: 80px;
    height: 8px;
    background: transparent;
    cursor: pointer;
  }

  .winamp-volume-slider::-webkit-slider-track {
    background: var(--winamp-slider-track);
    border: 1px solid var(--winamp-border);
    height: 6px;
    border-radius: 0;
  }

  .winamp-volume-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 10px;
    height: 16px;
    background: linear-gradient(to bottom, var(--winamp-slider-thumb-highlight) 0%, var(--winamp-slider-thumb) 50%, #808060 100%);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-slider-thumb-highlight);
    border-left: 1px solid var(--winamp-slider-thumb-highlight);
    cursor: pointer;
    box-shadow: inset -1px -1px 0 #606040;
  }

  .winamp-volume-slider::-moz-range-track {
    background: var(--winamp-slider-track);
    border: 1px solid var(--winamp-border);
    height: 6px;
    border-radius: 0;
  }

  .winamp-volume-slider::-moz-range-thumb {
    width: 10px;
    height: 16px;
    background: linear-gradient(to bottom, var(--winamp-slider-thumb-highlight) 0%, var(--winamp-slider-thumb) 50%, #808060 100%);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-slider-thumb-highlight);
    border-left: 1px solid var(--winamp-slider-thumb-highlight);
    cursor: pointer;
    box-shadow: inset -1px -1px 0 #606040;
    border-radius: 0;
  }

  .winamp-balance-slider {
    width: 80px;
    height: 8px;
    background: transparent;
    cursor: pointer;
  }

  .winamp-balance-slider::-webkit-slider-track {
    background: var(--winamp-slider-track);
    border: 1px solid var(--winamp-border);
    height: 6px;
    border-radius: 0;
  }

  .winamp-balance-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 10px;
    height: 16px;
    background: linear-gradient(to bottom, var(--winamp-slider-thumb-highlight) 0%, var(--winamp-slider-thumb) 50%, #808060 100%);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-slider-thumb-highlight);
    border-left: 1px solid var(--winamp-slider-thumb-highlight);
    cursor: pointer;
    box-shadow: inset -1px -1px 0 #606040;
  }

  .winamp-balance-slider::-moz-range-track {
    background: var(--winamp-slider-track);
    border: 1px solid var(--winamp-border);
    height: 6px;
    border-radius: 0;
  }

  .winamp-balance-slider::-moz-range-thumb {
    width: 10px;
    height: 16px;
    background: linear-gradient(to bottom, var(--winamp-slider-thumb-highlight) 0%, var(--winamp-slider-thumb) 50%, #808060 100%);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-slider-thumb-highlight);
    border-left: 1px solid var(--winamp-slider-thumb-highlight);
    cursor: pointer;
    box-shadow: inset -1px -1px 0 #606040;
  }

  .winamp-volume-display,
  .winamp-balance-display {
    font-size: 8px;
    color: var(--winamp-green);
    font-family: 'Courier New', monospace;
    min-width: 20px;
    text-align: center;
  }

  .winamp-mode-buttons {
    display: flex;
    gap: 4px;
    margin-bottom: 4px;
  }

  .winamp-mode-btn {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    color: var(--winamp-text);
    padding: 2px 6px;
    font-size: 8px;
    cursor: pointer;
    text-transform: lowercase;
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
  }

  .winamp-mode-btn.active {
    background: linear-gradient(to bottom, #80a080 0%, #608060 100%);
    color: var(--winamp-text-light);
    font-weight: bold;
    text-shadow: 0 1px 0 rgba(0, 0, 0, 0.3);
  }

  .winamp-toggle-buttons {
    display: flex;
    gap: 4px;
  }

  .winamp-toggle-btn {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    color: var(--winamp-text);
    padding: 2px 8px;
    font-size: 9px;
    font-weight: bold;
    cursor: pointer;
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
  }

  .winamp-toggle-btn.active {
    background: linear-gradient(to bottom, #80a080 0%, #608060 100%);
    color: var(--winamp-text-light);
    font-weight: bold;
    text-shadow: 0 1px 0 rgba(0, 0, 0, 0.3);
  }

  /* Controls */
  .winamp-controls {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 3px;
    padding: 6px;
    background: var(--winamp-bg);
    border-top: 1px solid var(--winamp-shadow);
  }

  .winamp-control-btn {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-highlight);
    border-left: 1px solid var(--winamp-highlight);
    padding: 6px 10px;
    font-size: 12px;
    font-weight: bold;
    cursor: pointer;
    min-width: 28px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--winamp-text);
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
    transition: all 0.05s ease;
  }

  .winamp-control-btn:hover:not(:disabled) {
    background: var(--winamp-light);
  }

  .winamp-control-btn:active:not(:disabled) {
    box-shadow: inset 1px 1px 2px rgba(0, 0, 0, 0.5);
    border-top: 1px solid var(--winamp-shadow);
    border-left: 1px solid var(--winamp-shadow);
    transform: translateY(1px);
  }

  .winamp-control-btn:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  .winamp-control-btn.active {
    background: linear-gradient(to bottom, #80a080 0%, #608060 100%);
    color: var(--winamp-text-light);
    font-weight: bold;
    text-shadow: 0 1px 0 rgba(0, 0, 0, 0.3);
    box-shadow: inset -1px -1px 0 #405040;
  }

  /* Equalizer Window */
  .winamp-equalizer {
    width: 275px;
    background: var(--winamp-bg);
    border: 2px solid var(--winamp-border);
    border-top: none;
    box-shadow: 
      inset -1px -1px 0 var(--winamp-shadow),
      inset 1px 1px 0 var(--winamp-highlight),
      2px 2px 4px rgba(0, 0, 0, 0.3);
  }

  .winamp-eq-controls {
    display: flex;
    gap: 4px;
    padding: 4px;
    background: var(--winamp-bg);
  }

  .winamp-eq-btn {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-highlight);
    border-left: 1px solid var(--winamp-highlight);
    padding: 2px 8px;
    font-size: 9px;
    font-weight: bold;
    cursor: pointer;
    color: var(--winamp-text);
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
  }

  .winamp-eq-btn:hover {
    background: var(--winamp-light);
  }

  .winamp-eq-btn:active {
    box-shadow: inset 1px 1px 2px rgba(0, 0, 0, 0.5);
    border-top: 1px solid var(--winamp-shadow);
    border-left: 1px solid var(--winamp-shadow);
  }

  .winamp-eq-btn.active {
    background: linear-gradient(to bottom, #80a080 0%, #608060 100%);
    color: var(--winamp-text-light);
    font-weight: bold;
    text-shadow: 0 1px 0 rgba(0, 0, 0, 0.3);
  }

  .winamp-eq-sliders {
    display: flex;
    padding: 8px;
    gap: 8px;
  }

  .winamp-eq-scale {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    font-size: 8px;
    color: var(--winamp-text);
    padding-right: 4px;
    height: 120px;
  }

  .winamp-eq-scale-label {
    text-align: right;
  }

  .winamp-eq-slider-group {
    display: flex;
    gap: 4px;
    flex: 1;
  }

  .winamp-eq-slider-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
  }

  .winamp-eq-slider-container label {
    font-size: 8px;
    color: var(--winamp-text);
    font-weight: bold;
  }

  .winamp-eq-slider {
    -webkit-appearance: slider-vertical;
    writing-mode: bt-lr;
    width: 20px;
    height: 100px;
    background: transparent;
    cursor: pointer;
  }

  .winamp-eq-slider::-webkit-slider-track {
    background: linear-gradient(to bottom, var(--winamp-slider-track), #2a4a2a);
    border: 1px solid var(--winamp-border);
    width: 8px;
    border-radius: 0;
  }

  .winamp-eq-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: 18px;
    height: 10px;
    background: linear-gradient(to bottom, var(--winamp-slider-thumb-highlight) 0%, var(--winamp-slider-thumb) 50%, #808060 100%);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-slider-thumb-highlight);
    border-left: 1px solid var(--winamp-slider-thumb-highlight);
    cursor: pointer;
    box-shadow: inset -1px -1px 0 #606040;
  }

  .winamp-eq-slider::-moz-range-track {
    background: linear-gradient(to bottom, var(--winamp-slider-track), #2a4a2a);
    border: 1px solid var(--winamp-border);
    width: 8px;
    border-radius: 0;
  }

  .winamp-eq-slider::-moz-range-thumb {
    width: 18px;
    height: 10px;
    background: linear-gradient(to bottom, var(--winamp-slider-thumb-highlight) 0%, var(--winamp-slider-thumb) 50%, #808060 100%);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-slider-thumb-highlight);
    border-left: 1px solid var(--winamp-slider-thumb-highlight);
    cursor: pointer;
    box-shadow: inset -1px -1px 0 #606040;
  }

  /* Playlist Window */
  .winamp-playlist {
    width: 275px;
    background: var(--winamp-bg);
    border: 2px solid var(--winamp-border);
    border-top: none;
    box-shadow: 
      inset -1px -1px 0 var(--winamp-shadow),
      inset 1px 1px 0 var(--winamp-highlight),
      2px 2px 4px rgba(0, 0, 0, 0.3);
    max-height: 300px;
    display: flex;
    flex-direction: column;
  }

  .winamp-playlist-content {
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 0;
  }

  .winamp-playlist-tracks {
    flex: 1;
    overflow-y: auto;
    padding: 2px;
    background: #fff;
    border: 1px inset var(--winamp-dark);
    margin: 4px;
    font-size: 10px;
    color: #000;
    max-height: 200px;
  }

  .winamp-playlist-track {
    padding: 2px 4px;
    cursor: pointer;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .winamp-playlist-track:hover {
    background: var(--winamp-light);
  }

  .winamp-playlist-track.active {
    background: var(--winamp-selected);
    color: var(--winamp-selected-text);
  }

  .winamp-playlist-controls {
    display: flex;
    gap: 4px;
    padding: 4px;
    background: var(--winamp-bg);
  }

  .winamp-playlist-btn {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-highlight);
    border-left: 1px solid var(--winamp-highlight);
    padding: 2px 6px;
    font-size: 8px;
    font-weight: bold;
    cursor: pointer;
    color: var(--winamp-text);
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
  }

  .winamp-playlist-btn:hover {
    background: var(--winamp-light);
  }

  .winamp-playlist-btn:active {
    box-shadow: inset 1px 1px 2px rgba(0, 0, 0, 0.5);
    border-top: 1px solid var(--winamp-shadow);
    border-left: 1px solid var(--winamp-shadow);
  }

  .winamp-playlist-summary {
    padding: 2px 8px;
    font-size: 9px;
    color: var(--winamp-text);
    background: var(--winamp-bg);
    border-top: 1px solid var(--winamp-shadow);
    text-align: center;
  }

  .winamp-playlist-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 4px;
    background: var(--winamp-bg);
    border-top: 1px solid var(--winamp-shadow);
  }

  .winamp-mini-controls {
    display: flex;
    gap: 2px;
  }

  .winamp-mini-btn {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-highlight);
    border-left: 1px solid var(--winamp-highlight);
    padding: 2px 4px;
    font-size: 8px;
    font-weight: bold;
    cursor: pointer;
    min-width: 18px;
    height: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--winamp-text);
    box-shadow: inset -1px -1px 0 var(--winamp-shadow);
  }

  .winamp-mini-btn:hover {
    background: var(--winamp-light);
  }

  .winamp-mini-btn:active {
    box-shadow: inset 1px 1px 2px rgba(0, 0, 0, 0.5);
    border-top: 1px solid var(--winamp-shadow);
    border-left: 1px solid var(--winamp-shadow);
  }

  .winamp-mini-btn.active {
    background: linear-gradient(to bottom, #80a080 0%, #608060 100%);
    color: var(--winamp-text-light);
    font-weight: bold;
    text-shadow: 0 1px 0 rgba(0, 0, 0, 0.3);
  }

  /* Scrollbar styling for playlist */
  .winamp-playlist-tracks::-webkit-scrollbar {
    width: 16px;
  }

  .winamp-playlist-tracks::-webkit-scrollbar-track {
    background: var(--winamp-button-bg);
    border: 1px solid var(--winamp-border);
  }

  .winamp-playlist-tracks::-webkit-scrollbar-thumb {
    background: var(--winamp-dark);
    border: 1px solid var(--winamp-border);
    border-top: 1px solid var(--winamp-highlight);
    border-left: 1px solid var(--winamp-highlight);
  }

  .winamp-playlist-tracks::-webkit-scrollbar-thumb:hover {
    background: var(--winamp-darker);
  }

  /* Ensure player stays in place during transitions */
  [data-astro-transition-scope="music-player"] {
    position: fixed !important;
    z-index: 9999 !important;
    pointer-events: auto !important;
  }

  /* Prevent Astro view transitions from affecting the player */
  .winamp-player,
  .winamp-player * {
    view-transition-name: none !important;
  }

  @media (max-width: 768px) {
    .winamp-main,
    .winamp-equalizer,
    .winamp-playlist {
      width: 100%;
      max-width: 320px;
    }

    .winamp-controls {
      flex-wrap: wrap;
      gap: 2px;
    }

    .winamp-control-btn {
      min-width: 24px;
      padding: 4px 6px;
      font-size: 10px;
    }

    .winamp-volume-balance-container {
      flex-direction: column;
      gap: 8px;
    }

    .winamp-volume-slider,
    .winamp-balance-slider {
      width: 100%;
    }

    .winamp-time-display {
      font-size: 16px;
    }

    .winamp-digit {
      min-width: 10px;
      padding: 1px 2px;
    }
  }
</style>
