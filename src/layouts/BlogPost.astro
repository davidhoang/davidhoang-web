---
import MainLayout from './MainLayout.astro';
import BlogMeta from '../components/BlogMeta.astro';
import { Image } from 'astro:assets';
import SidebarWriting from '../components/SidebarWriting.astro';
import CommentsSection from '../components/CommentsSection.astro';

const { data } = Astro.props;
const { title, description, pubDate } = data;

// Generate a room ID based on the post title/slug
const roomId = title.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '');
---

<MainLayout title={title}>
  <BlogMeta 
    title={title}
    description={description}
    ogImage={data.ogImage}
    publishDate={pubDate}
    url={Astro.url.pathname}
  />
  <article class="blog-post">
    <div class="container">
      <div class="content-grid">
        <div class="main-content">
          <h1>{title}</h1>
          <time datetime={pubDate}>
            {new Date(pubDate).toLocaleDateString()}
          </time>
          <div class="prose-content">
            <slot />
          </div>
          <CommentsSection roomId={roomId} />
        </div>
        <SidebarWriting />
      </div>
    </div>
  </article>
</MainLayout>

<style>
  .content-grid {
    display: grid;
    grid-template-columns: 3fr 1fr;
    gap: 3rem;
  }

  .post-image {
    margin: 2rem 0;
  }
  
  .post-image img {
    width: 100%;
    height: auto;
    border-radius: 8px;
  }
  
  time {
    color: #666;
    font-size: 0.9rem;
    display: block;
    margin: 1rem 0;
  }

  @media (max-width: 768px) {
    .content-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
